import { TransformPipePipe } from './transform-pipe.pipe';

describe('TransformPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TransformPipePipe();
    expect(pipe).toBeTruthy();
  });
});
